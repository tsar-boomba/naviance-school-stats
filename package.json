{
	"name": "naviance-school-stats",
	"version": "0.0.1",
	"description": "Extracts your school's application stats (acceptances, etc.) from naviance.",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"dev": "vite & tsup --watch ./src/sw.ts",
		"build": "tsc && vite build && tsup",
		"build-dev": "vite build",
		"preview": "vite preview",
		"package": "npm run build && zip -r extension.zip dist"
	},
	"dependencies": {
		"@emotion/react": "11.10.5",
		"@mantine/core": "5.7.1",
		"@mantine/hooks": "5.7.1",
		"@tabler/icons": "^1.110.0",
		"preact": "10.11.2",
		"prettier": "2.7.1"
	},
	"devDependencies": {
		"@preact/preset-vite": "2.4.0",
		"chrome-types": "^0.1.145",
		"tsup": "6.4.0",
		"typescript": "4.8.4",
		"vite": "3.2.0"
	}
}
